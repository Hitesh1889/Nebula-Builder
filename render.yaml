services:
  - type: web
    name: visinaro
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: GEMINI_API_KEY
        sync: false   # Set this once in Render Dashboard → Environment
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
